import React from 'react';
import { View, Text } from 'react-native';

const GetScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>GET Screen</Text>
      {/* Add GET-related content here */}
    </View>
  );
};

export default GetScreen;
